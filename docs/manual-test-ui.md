# 签字页管理系统 — UI 手动测试指南

> **测试方式**：浏览器操作，人工核查  
> **前置条件**：后端运行中（`cd backend && uv run uvicorn app.main:app --port 8000`），前端运行中（`cd frontend && npm run dev`）  
> **测试数据**：全部使用真实场景数据，逐条验证

---

## 目录

1. [项目列表页操作](#1-项目列表页操作)
2. [创建项目并进入详情](#2-创建项目并进入详情)
3. [Step 1 — 模板选择](#3-step-1--模板选择)
4. [Step 2 — 变量填写](#4-step-2--变量填写)
5. [Step 3 — 生成下载](#5-step-3--生成下载)
6. [上传自定义模板（AI 解析全流程）](#6-上传自定义模板ai-解析全流程)
7. [AI 去重与校验](#7-ai-去重与校验)
8. [Excel 导入导出](#8-excel-导入导出)
9. [异常场景与容错验证](#9-异常场景与容错验证)
10. [完整黄金流程](#10-完整黄金流程)

---

## 1. 项目列表页操作

### 1.1 打开首页

打开浏览器访问 `http://localhost:5173`

**预期**：看到项目列表页，包含：
- 页面标题：签字页管理系统
- 右上角「新建项目」按钮
- 居中区域：项目列表（当前为空，显示"暂无项目"提示）

### 1.2 创建第一个项目

1. 点击「新建项目」按钮
2. 弹窗中输入项目名称：**海通证券 IPO 法律意见书签字页项目**
3. 点击「确认」

**预期**：
- 列表中出现该项目卡片
- 卡片显示：项目名称、状态为 `draft`、创建时间

### 1.3 批量创建项目（供列表测试用）

重复上述步骤，再创建两个项目：

| 项目名称 | 预期状态 |
|---------|---------|
| 中芯国际股东大会签字页项目 | `draft` |
| 字节跳动融资签字页项目 | `draft` |

**预期**：列表显示 3 个项目，按 `updated_at` 降序排列。

### 1.4 删除项目

1. 找到「字节跳动融资签字页项目」的卡片
2. 点击卡片右下角的「删除」按钮（垃圾桶图标）
3. 确认删除

**预期**：
- 项目从列表中消失
- Toast 提示"删除成功"
- 列表剩余 2 个项目

### 1.5 列表为空状态

删除所有项目，验证空状态展示。

**预期**：页面显示"暂无项目"及「新建项目」引导按钮（可选验证项，非必需）。

---

## 2. 创建项目并进入详情

### 2.1 创建主测试项目

```json
项目名称：海通证券 IPO 法律意见书签字页项目
```

### 2.2 进入项目详情

点击项目卡片，进入详情页。

**预期**：进入三步流程页面，左侧显示步骤导航：
- Step 1: 选择模板（当前高亮）
- Step 2: 填写变量（灰色，未解锁）
- Step 3: 生成下载（灰色，未解锁）

---

## 3. Step 1 — 模板选择

### 3.1 查看模板列表

模板库应展示 **3 个预置模板**：

| 模板名称 | 预期标记 | 变量数 |
|---------|---------|--------|
| 律所签字页 | 预置 · IPO 法律意见书 | 8 个 |
| 自然人股东签字页 | 预置 · 股东大会决议 | 6 个 |
| 机构股东签字页 | 预置 · 股东大会决议 | 6 个 |

每个模板卡片包含：
- 模板名称
- 描述
- 分类标签
- 适用场景
- 变量数量
- 已选中/可选择状态

### 3.2 搜索和筛选模板

1. 在搜索框输入 **"法律"**
2. **预期**：只展示"律所签字页"一个结果
3. 清空搜索框

1. 点击分类筛选下拉
2. 选择"股东大会"分类
3. **预期**：只展示自然人/机构股东两个模板
4. 重置筛选

### 3.3 多选模板

1. 选择 **律所签字页**（点击卡片或复选框）
2. 选择 **机构股东签字页**
3. 点击「下一步」

**预期**：
- 弹出成功提示：已为项目选择 2 个模板
- 变量自动去重
- 页面自动流转到 Step 2

### 3.4 展开查看模板详情

在模板卡片上点击「展开详情」：

**预期**：显示该模板的完整变量列表和元数据：
| 模板 | 展开后可见变量 |
|------|--------------|
| 律所签字页 | target_company_name, law_firm_name, law_firm_director, handling_lawyer, signing_date, exchange_name, document_type, target_investor_type |
| 机构股东签字页 | institutional_shareholder_name, authorized_representative_name, target_company_name, meeting_year, meeting_session, signing_date |

### 3.5 回退验证

1. 回到 Step 1
2. **预期**：之前选择的两个模板仍然显示已选中状态
3. 回到 Step 2（数据不丢失）

---

## 4. Step 2 — 变量填写

### 4.1 验证去重结果

进入 Step 2，变量列表应包含 **13 个去重后变量**（律所签字页 8 个 + 机构股东签字页 6 个 - 2 个共享变量 target_company_name + signing_date = 12，加上 handling_lawyer 多值拆 2 行 = 13）：

变量按 `category` 分组，组内按 `sort_order` 排列：

| 分组 | 变量 | 预期特征 |
|------|------|---------|
| **company**（公司） | target_company_name | 必填，`source_template_ids` 显示来自 2 个模板（合并标识） |
| | exchange_name | 非必填 |
| | institutional_shareholder_name | 必填 |
| **lawyer**（律所） | law_firm_name | 必填 |
| | law_firm_director | 必填 |
| | handling_lawyer_1 | 必填，多值，默认 2 行 |
| | handling_lawyer_2 | 必填，多值 |
| **shareholder**（股东） | authorized_representative_name | 必填 |
| **meeting**（会议） | meeting_year | 必填 |
| | meeting_session | 必填 |
| **document**（文档） | document_type | 非必填 |
| | target_investor_type | 非必填 |
| **date**（日期） | signing_date | 必填，合并标识 |

去重合并标记验证：
- `target_company_name` 有**合并标记**（is_merged），说明来自律所签字页 + 机构股东签字页
- `signing_date` 同样有合并标记
- `law_firm_name` 没有合并标记（仅来自律所签字页一份）

### 4.2 填写变量值

逐组填写以下数据：

| 分组 | 变量 | 填入的值 |
|------|------|---------|
| **company** | target_company_name | 海通证券股份有限公司 |
| | exchange_name | 上海证券交易所 |
| | institutional_shareholder_name | 金石投资有限公司 |
| **lawyer** | law_firm_name | 君合律师事务所上海分所 |
| | law_firm_director | 张明 |
| | handling_lawyer_1 | 王芳 |
| | handling_lawyer_2 | 李强 |
| **shareholder** | authorized_representative_name | 赵刚 |
| **meeting** | meeting_year | 2026 |
| | meeting_session | 第一次临时 |
| **document** | document_type | 法律意见书 |
| | target_investor_type | 合格机构投资者 |
| **date** | signing_date | 2026年6月21日 |

### 4.3 多值变量动态增删行

1. 找到 `handling_lawyer` 分组
2. 点击「+ 添加」按钮
3. **预期**：新增一行 `handling_lawyer_3`
4. 在新增行填入：**陈伟**
5. 点击 `handling_lawyer_3` 行末的「删除」按钮
6. **预期**：该行被移除，剩余 2 行

### 4.4 实时校验反馈

1. 将 `signing_date` 的值改为 **2026/06/21**（错误格式）
2. **预期**：输入框边框变红，下方显示格式错误提示
3. 改回 **2026年6月21日**
4. **预期**：红色消失

1. 将 `target_company_name` 的值改为 **海通证券**
2. **预期**：显示校验提示"公司名可能不完整，建议填写全称（含股份有限公司/有限责任公司）"
3. 改回 **海通证券股份有限公司**

### 4.5 填写完跳转

1. 确认所有必填变量已填写
2. 点击「下一步」

**预期**：
- 数据自动保存（调用 PUT /variables API）
- 进入 Step 3

### 4.6 回退验证（不丢数据）

1. 点击"上一步"回到 Step 2
2. **预期**：所有已填写的变量值仍然保留
3. 再回到 Step 3

---

## 5. Step 3 — 生成下载

### 5.1 生成前确认弹窗

1. 进入 Step 3 后，点击「生成文档」按钮
2. **预期**：弹出确认弹窗，显示变量 key→value 对照表

| 变量 | 值 |
|------|-----|
| target_company_name | 海通证券股份有限公司 |
| law_firm_name | 君合律师事务所上海分所 |
| law_firm_director | 张明 |
| handling_lawyer_1 | 王芳 |
| handling_lawyer_2 | 李强 |
| signing_date | 2026年6月21日 |
| exchange_name | 上海证券交易所 |
| document_type | 法律意见书 |
| target_investor_type | 合格机构投资者 |
| institutional_shareholder_name | 金石投资有限公司 |
| authorized_representative_name | 赵刚 |
| meeting_year | 2026 |
| meeting_session | 第一次临时 |

### 5.2 确认生成

1. 在确认弹窗中点击「确认生成」
2. **预期**：
   - 弹窗关闭
   - 按钮变为"生成中..."（加载中状态）
   - 按钮旁出现取消按钮

### 5.3 轮询进度

观察进度指示器：

| 阶段 | 预期展示 |
|------|---------|
| 生成开始 | 进度条显示 0%，显示"正在为模板 1/2 生成..." |
| 第 1 个模板完成 | 进度条 50%，"律所签字页 ✓" |
| 第 2 个模板完成 | 进度条 100%，"全部完成" |

轮询逻辑验证：前端应使用指数退避轮询（2s → 4s → 8s → max 10s）。

### 5.4 查看生成文件列表

1. 生成完成后，页面显示文件列表
2. **预期**：列出 2 个生成文件：

| 文件名 | 状态 |
|-------|------|
| 律所签字页_*.docx | completed |
| 机构股东签字页_*.docx | completed |

### 5.5 下载单个文件

1. 点击「律所签字页_*.docx」的下载按钮
2. **预期**：浏览器下载一个 Word 文件

**人工验证**：
```bash
# 终端验证下载的文件是有效 .docx
python3 -c "
from zipfile import ZipFile
import glob
f = list(glob.glob('~/Downloads/*.docx'))[0]
with ZipFile(f) as z:
    assert '[Content_Types].xml' in z.namelist()
    print(f'✓ {f} 是有效 DOCX ({len(z.namelist())} 个内部文件)')
"
```

### 5.6 打包下载全部

点击「打包下载全部」按钮

**预期**：
- 浏览器下载一个 ZIP 文件
- 解压后包含 2 个 .docx 文件
- 文件名格式：`海通证券 IPO 法律意见书签字页项目_all_20260621_xxxxxx.zip`

### 5.7 取消生成测试

> 建议创建一个新项目单独测试取消功能，避免影响主测试项目的数据完整性。

1. 回到项目列表页
2. 新建项目：**取消测试项目**
3. 进入详情 → Step 1 → 选择 2~3 个模板
4. 跳转到 Step 3
5. 点击「生成文档」
6. **立即**点击「取消生成」

**预期**：
- 取消按钮响应
- 进度停止
- 页面显示"已取消"状态
- 已生成的部分文件被清理

### 5.8 禁止重复生成验证

1. 取消后，再次点击「生成文档」
2. **预期**：检查是否正常重新生成（取消后的项目应允许重新生成）

---

## 6. 上传自定义模板（AI 解析全流程）

### 6.1 进入上传流程

在 Step 1 模板选择页面，点击底部「上传自定义模板」按钮。

**预期**：弹窗打开，标题"创建自定义模板"，包含文件上传区域。

### 6.2 上传并 AI 解析

1. 选择文件 → 上传 `backend/app/templates/law_firm_signing_page.docx`（预置模板文件）
2. **预期**：
   - 上传按钮显示加载动画（`AiLoadingPanel`）
   - 等待 1~3 秒
   - AI 解析完成后弹窗显示解析结果：

| 变量 | label | 可信度 | 操作 |
|------|-------|--------|------|
| law_firm_name | 律师事务所名称 | 高可信 98% | ✓ 已注册 |
| law_firm_director | 律师事务所负责人 | 高可信 95% | ✓ 已注册 |
| handling_lawyer | 经办律师 | 高可信 92% | ✓ 已注册 |

每条变量卡片应展示：
- 左上角：`TrustLevelBadge`（高可信 / 待确认 / 需审核）
- 右上角：`ConfidenceMeter`（绿色 ≥80% / 黄色 ≥50% / 红色 <50%）
- 注册标记（如适用）
- 提取依据（可展开）：`[▼ 提取依据（1条）]` → 展开显示原文片段
- 风险说明（如适用）

### 6.3 信任等级卡片颜色验证

| 等级 | 边框颜色 | 文字 |
|------|---------|------|
| 高可信 (high) | 默认（无特殊边框） | "高可信" |
| 待确认 (medium) | 主色边框（primary） | "待确认" |
| 需审核 (low) | 红色边框（destructive） | "需审核" → 不可直接创建 |

### 6.4 编辑 AI 解析结果

1. 尝试修改某个变量的 key
2. **预期**：每个字段都可编辑（key 和 label 都是输入框）
3. 将 `law_firm_name` 的 key 改为 `firm_name`、label 改为「律所名称」
4. 编辑器自动将其 trust_level 从 "high" 升为 "medium"（表示已人工处理）

### 6.5 采纳建议

将 key 改回 `law_firm_name`、label 改回「律师事务所名称」。

### 6.6 LOW 审核阻断验证

1. 删除某个变量的 key 内容
2. 点击「确定创建」
3. **预期**：被拒绝，toast 提示"存在需审核的变量，请修正 key、采纳建议或删除后再创建"

### 6.7 确认创建模板

1. 修正所有 LOW 变量
2. 点击「确定创建」
3. **预期**：
   - 弹窗关闭
   - 模板列表中新增自定义模板：**君合律所专用签字页模板**
   - 标记为"自定义"
   - 变量数显示为 4

### 6.8 移除模板测试

1. 在已选模板列表中，找到自定义模板
2. 点击「移除」按钮
3. **预期**：
   - 模板从已选列表消失
   - 对应的变量行被清理（Variable 表中 `source_template_ids` 移除该模板 ID）
   - 如果变量仅来自该模板，被彻底删除
   - 共享变量不受影响（target_company_name 等）

### 6.9 重新添加自定义模板

1. 重新从模板库选择该自定义模板
2. **预期**：可正常添加，变量重新生成

---

## 7. AI 去重与校验

### 7.1 进入 Step 2

确保主测试项目（海通证券 IPO）处于 Step 2。

### 7.2 AI 去重

1. 点击「AI 智能去重」按钮
2. **预期**：
   - 按钮显示加载动画
   - 完成后弹出 `DedupSuggestionsPanel` 面板
   - 如果 LLM 不可用，显示 `AiDegradedBanner` 降级提示

**如果 AI 正常运行，展示去重建议：**

每条建议卡片包含：
| 元素 | 预期展示 |
|------|---------|
| 建议内容 | "xxx 与 yyy 是同一个字段，建议合并" |
| TrustLevel 徽章 | 高可信 / 待确认 / 需审核 |
| 置信度 | 如 "置信度 85%" |
| 采纳按钮 | ✅ 采纳（适用于确认合并）|
| 拒绝按钮 | ❌ 拒绝（适用于忽略该建议）|

### 7.3 应用去重建议

1. 点击某条建议的「采纳」按钮
2. **预期**：
   - 该建议被标记为"已采纳"
   - 建议卡片变为不可操作状态
   - 变量列表刷新，被合并的变量消失
   - `ApplyDedupResponse` 返回 `merged_rows`

### 7.4 AI 校验

1. 点击「AI 数据校验」按钮
2. **预期**：
   - 按钮显示加载动画
   - 完成后弹出 `ValidationReportPanel` 面板
   - 以当前填写的数据为准，应无 error，可能有 0 条 issue

### 7.5 校验结果展示

如果数据完整，预期面板头部显示：
```
✓ 校验通过，未发现问题
```

面板包含两种级别的 issue：
| 级别 | 颜色 | 图标 |
|------|------|------|
| error | 红色 | 🚫 |
| warning | 黄色/主色 | ⚠ |

### 7.6 触发校验错误

1. 回到 Step 2
2. 将 `signing_date` 的值改为 **2026/06/21**（非法格式）
3. 再次点击「AI 数据校验」
4. **预期**：
   - `regex_issues` 至少包含一条 error：
     - `variable_key: signing_date`
     - `message: "变量 signing_date 的值不符合格式要求"`
     - `source: "regex"`
   - 点击 issue 可定位到对应的输入框（滚动 + 高亮）
5. 改回 **2026年6月21日**

---

## 8. Excel 导入导出

### 8.1 导出 Excel 模板

在 Step 2 页面：
1. 点击「导出模板」
2. **预期**：下载一个 `.xlsx` 文件，包含表头和数据行

**人工验证：**
```bash
python3 -c "
from openpyxl import load_workbook
wb = load_workbook('~/Downloads/*.xlsx')
ws = wb.active
print('表头:', [c.value for c in next(ws.iter_rows(max_row=1))])
print('行数:', ws.max_row - 1)
"
```
**预期表头**：`变量标识(key)` | `变量名称(label)` | `值(value)`

### 8.2 导出已填数据

1. 点击「导出数据」
2. **预期**：下载 `.xlsx` 文件，所有已填写变量值都在其中
3. 人工确认 `target_company_name` 行的值列显示 **海通证券股份有限公司**

### 8.3 Excel 导入预览

1. 准备好导入文件 `/tmp/import_test.xlsx`（可在终端制作）：

```bash
python3 -c "
from openpyxl import Workbook
wb = Workbook()
ws = wb.active
ws.title = '变量'
ws.append(['变量标识(key)', '变量名称(label)', '值(value)'])
ws.append(['target_company_name', '目标公司名称', '海通证券股份有限公司'])
ws.append(['law_firm_name', '律师事务所名称', '君合律师事务所'])
ws.append(['signing_date', '签署日期', '2026年12月31日'])
ws.append(['nonexistent_key', '不存在的变量', 'test'])
wb.save('/tmp/import_test.xlsx')
"
```

2. 在 Step 2 点击「导入 → 预览」
3. 选择 `/tmp/import_test.xlsx`
4. **预期**：显示预览结果：

| 行 | key | value | 状态 |
|----|-----|-------|------|
| 2 | target_company_name | 海通证券股份有限公司 | ✅ 通过（3 行）|
| 3 | law_firm_name | 君合律师事务所 | ✅ 通过 |
| 4 | signing_date | 2026年12月31日 | ✅ 通过 |
| 5 | nonexistent_key | test | ❌ 不存在于项目 |

### 8.4 确认导入

1. 在预览结果中，确认有效的 3 行
2. 点击「确认导入」
3. **预期**：
   - Toast 提示导入成功
   - 变量列表刷新，三个变量的值被更新
   - `target_company_name` 变为 **海通证券股份有限公司**
   - `law_firm_name` 变为 **君合律师事务所**

---

## 9. 异常场景与容错验证

### 9.1 AI 降级测试

1. 前端设置 Mock 模式，或者直接拔掉网络
2. 上传自定义模板
3. **预期**：
   - `ParsedVariablesEditor` 顶部显示 `AiDegradedBanner`
   - 变量列表为空
   - 提示"AI 服务不可用，请使用手工操作或稍后重试"
   - 用户可手动添加变量（展示「添加变量」按钮）

### 9.2 空模板创建阻断

1. 不选任何模板，直接点击「下一步」
2. **预期**：按钮不可点击，或点击后提示"请至少选择一个模板"

### 9.3 必填变量未填阻断

1. 在 Step 2 清空某个必填变量
2. 点击「下一步」
3. **预期**：阻止流转，提示"存在必填变量未填写"

### 9.4 模板已选不可重复选

在 Step 1，已选模板的卡片应显示为选中状态，不可重复选中（或选中无变化）。

### 9.5 浏览器刷新不丢数据

1. 在 Step 2 填写部分变量值
2. 手动刷新浏览器（F5）
3. **预期**：停留在当前步骤，已填写的变量值可见

### 9.6 beforeunload 提示

1. 在 Step 2 填写了部分变量但未保存
2. 点击浏览器关闭/刷新
3. **预期**：浏览器弹出"确定离开？未保存的数据可能丢失"提示

---

## 10. 完整黄金流程

### 端到端操作步骤

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 打开 `http://localhost:5173` | 看到项目列表页 |
| 2 | 新建项目「海通证券 IPO 签字页项目」 | 项目创建成功，进入详情页 |
| 3 | Step 1 → 选择「律所签字页」+「机构股东签字页」 | 选中 2 个模板，自动进入 Step 2 |
| 4 | Step 2 → 填写所有变量值（13 个） | 必填变量全部填写完成 |
| 5 | Step 2 → 点击「下一步」 | 进入 Step 3 |
| 6 | Step 3 → 点击「生成文档」→ 确认 | 生成任务创建，等待完成 |
| 7 | 等待进度条走完 | 2 个模板生成完成 |
| 8 | 点击文件列表中的下载按钮 | 下载有效 .docx 文件 |
| 9 | 打开下载的 .docx | 签字页格式正确，变量值已替换 |
| 10 | 验证 ZIP 下载 | 打包下载包含 2 个 .docx |

### 生成文件验证

打开下载的 .docx 文件，确认：

**律所签字页（打开 docx 人工核对）**：
| 占位符位置 | 预期文字 |
|-----------|---------|
| 律师所名称处 | 君合律师事务所上海分所 |
| 负责人处 | 张明 |
| 经办律师处 | 王芳、李强 |
| 目标公司处 | 海通证券股份有限公司 |
| 签署日期处 | 2026年6月21日 |

**机构股东签字页**：
| 占位符位置 | 预期文字 |
|-----------|---------|
| 机构名称处 | 金石投资有限公司 |
| 授权代表处 | 赵刚 |
| 股东大会年份 | 2026 |
| 股东大会次数 | 第一次临时 |
| 签署日期处 | 2026年6月21日 |
| 目标公司处 | 海通证券股份有限公司 |

---

## 测试结果记录模板

| 用例编号 | 用例名 | 测试结果 (PASS/FAIL) | 备注 |
|---------|--------|-------------------|------|
| 1.1 | 首页展示 | | |
| 1.2 | 创建项目 | | |
| 2.1 | 进入详情三步流程 | | |
| 3.3 | 多选模板 | | |
| 4.2 | 填写变量 | | |
| 5.5 | 下载单文件 | | |
| 6.2 | AI 解析模板 | | |
| 6.6 | LOW 审核阻断 | | |
| 7.6 | 触发校验错误 | | |
| 8.3 | Excel 导入预览 | | |
| 9.1 | AI 降级 | | |
| 10 | 端到端黄金流程 | | |
